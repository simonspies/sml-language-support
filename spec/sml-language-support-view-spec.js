'use babel';

import SmlLanguageSupportView from '../lib/sml-language-support-view';

describe('SmlLanguageSupportView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
